#![allow(unused)]
use crate::lattice::Level;

// Repacks a level's children array to the minimum bit width needed to address
// the pool. Called after tree construction when the node count is known.
pub fn repack_level_children(level: &mut Level) {
	todo!()
}
