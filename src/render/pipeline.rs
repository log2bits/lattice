/// Compute pipeline: primary ray shader, bind group layout.
pub struct RenderPipeline {
	// wgpu pipeline and bind groups
}

impl RenderPipeline {
	pub fn new(device: &wgpu::Device) -> Self {
		todo!()
	}
}
